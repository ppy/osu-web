apiVersion: v1
kind: Service
metadata:
  name: {{ include "osu-web-chart.fullname" . }}
  labels:
    {{- include "osu-web-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "osu-web-chart.selectorLabels" . | nindent 4 }}
